# NOTICE — Fork HI-HA de LearnHouse

Ce dépôt est un **fork de [LearnHouse](https://github.com/learnhouse/learnhouse)**,
logiciel libre sous licence **GNU AGPL-3.0** (voir [LICENSE](./LICENSE), conservé intégralement).
Copyright © LearnHouse et ses contributeurs.

- **Dépôt d'origine** : https://github.com/learnhouse/learnhouse
- **Révision d'origine** : release `1.3.5` (`upstream/main`, commit `7369713e`, mergée le 2026-09-01 ; fork initial depuis la release `1.3.4`, tag `1.3.4`, commit `fb5196517706d0179c1b9747c47d42e307990560`, publiée le 2026-07-27)
- **Date du fork** : 2026-08-04
- **Exploité par** : HI-HA — https://campus.hi-ha.be

## Modifications apportées par rapport à l'origine

> Cette liste est tenue à jour à chaque changement, conformément à l'AGPL-3.0 (art. 5a).

| Date | Modification |
|------|--------------|
| 2026-08-04 | Ajout de `NOTICE-HIHA.md` (ce fichier) et `README-HIHA.md` (procédure de synchronisation upstream). Aucune modification du code ni des mentions légales. |
| 2026-08-06 | Ajout de l'infrastructure de déploiement, sans aucune modification du code applicatif : `.gitlab-ci.yml` (build de l'image et déploiement), `docker-compose.hiha.local.yml` et `.env.hiha.local.example` (environnement de test local). |
| 2026-08-07 | **Première modification de code** — `docker/nginx.conf` : le nginx interne de l'image écrasait l'en-tête `X-Forwarded-Proto` par sa propre variable `$scheme` (8 occurrences). Derrière un proxy de bordure qui termine le TLS (Traefik), l'API recevait donc toujours `http` et émettait les cookies de session **sans l'attribut `Secure`** (`apps/api/src/routers/auth.py`, `is_request_secure`), tandis que les URL canoniques sortaient en `http://`. Correctif : une directive `map` propage l'en-tête entrant lorsqu'il vaut `https` ou `http`, et retombe sur `$scheme` en son absence (exécution locale ou accès direct). Aucun changement de comportement hors reverse-proxy. |
| 2026-08-07 | **Changement de marque (rebranding HI-HA), 1re passe.** Remplacement du contenu de 6 fichiers d'images, à noms de fichiers inchangés : `apps/web/public/lrn.svg`, `lrn-dash.svg`, `lrn-text.svg`, `hiha-mark.png` (renommé depuis `learnhouse_bigicon_1.png`), `black_logo.png`, `favicon.ico`. Remplacement de libellés visibles (`alt`, titres de page, valeurs de repli) dans 22 composants, et des chaînes de l'écran de connexion (`auth.image_title_login`, `auth.image_title_signup`, `auth.terms_text`) dans les 22 fichiers de langue. Les logos de l'écran de connexion, du pied de page d'organisation et du filigrane pointent désormais vers l'instance elle-même (`/`) au lieu de `learnhouse.app`. |
| 2026-08-07 | `apps/web/components/Footers/LegalFooters.tsx` — deux corrections. (1) Les liens « Terms of Service » / « Privacy Policy » pointaient en dur vers `learnhouse.io` : sur une instance auto-hébergée, cela affirme à tort que les utilisateurs contractent avec LearnHouse, Inc. Ils ne s'affichent plus que si ce déploiement publie effectivement de telles pages (URL de plateforme configurée). (2) **Ajout du lien « Code source » (AGPL art. 13)** vers le miroir public, afin que les utilisateurs du service par le réseau se voient réellement offrir la source. URL surchargeable par `NEXT_PUBLIC_LEARNHOUSE_SOURCE_URL`. |
| 2026-08-07 | `apps/web/components/Emails/LearnHouseEmail.tsx` — logo LearnHouse hotlinké depuis `learnhouse.io` remplacé par un logo servi par l'instance, signature configurable. ⚠️ **Portée réelle de cette entrée** : ce composant n'est utilisé que par le chemin de facturation SaaS (`apps/web/services/emails/resend.ts`, qui sort immédiatement hors mode SaaS). Il **ne concerne pas** les emails d'invitation et de réinitialisation de mot de passe d'un déploiement auto-hébergé, qui sont produits par le backend Python — voir l'entrée suivante. Une première rédaction de cette ligne affirmait à tort le contraire ; elle est rectifiée ici conformément à l'art. 5a. |
| 2026-08-07 | **Changement de marque, 2e passe — surfaces manquées par la 1re.** *Backend* : `apps/api/src/services/users/emails.py` (le wordmark LearnHouse était inliné en SVG dans l'en-tête de **chaque** email d'invitation et de réinitialisation ; remplacé par un logo PNG servi par l'instance — le module notait lui-même que le SVG inline est supprimé par certains clients de messagerie — avec repli sur le nom du site en texte ; `ACADEMY_URL` ne renvoie plus vers `university.learnhouse.io`), `apps/api/src/services/email/utils.py` (le nom d'expéditeur de tous les emails suit désormais `site_name`), `apps/api/src/services/email/translations.py` (184 chaînes, 22 langues), `apps/api/config/config.yaml` (`site_name`, `site_description`, `contact_email`), `apps/api/src/routers/admin.py` (titre et contact de la page d'erreur publique du lien magique), `apps/api/src/services/auth/mfa.py` (`TOTP_ISSUER`, inscrit durablement dans l'application d'authentification des utilisateurs), noms des archives d'export, message d'erreur d'import, en-tête `User-Agent` des webhooks sortants, préfixe des enregistrements DNS de vérification de domaine, invite du planificateur de cours IA. *Frontend* : `/home` (« Powered by LearnHouse » codé en dur), `/admin` et `/admin/login`, titre du tableau de bord, titres des pages d'authentification, description Open Graph des pages de cours publiques, fichier de codes de secours 2FA, 217 chaînes de marque dans les 22 fichiers de langue, remplacement des images `learnhouse_ai_*`, `lrnai_icon.png`, `UNI_LOGO.png`, `theclassroom.png`, et suppression de 5 images de marque devenues inutilisées. **Volontairement conservés car factuellement exacts** : les libellés désignant le *format d'export LearnHouse* (format d'interopérabilité réel) et les liens vers la documentation amont dans les menus d'administration. |
| 2026-08-07 | `apps/web/components/Objects/Menus/OrgMenu.tsx` — le texte alternatif du logo **de l'organisation** (marque blanche) annonçait le nom du produit ; il suit désormais le nom de l'organisation, sans quoi un lecteur d'écran annonçait « HI-HA » pour le logo d'un tiers. |
| 2026-08-07 | `apps/web/components/Objects/StyledElements/Error/ErrorActions.tsx` — le bouton « Contact support » de toutes les pages d'erreur retombait sur `mailto:support@learnhouse.io` dès qu'aucune URL de plateforme n'est configurée, ce qui est précisément notre cas. Le bouton est désormais masqué plutôt que de diriger nos utilisateurs vers le support d'un tiers. |
| 2026-08-07 | **Ligne de copyright — double attribution.** Une première rédaction remplaçait `© {{year}} LearnHouse, Inc.` par `© {{year}} HI-HA` dans les 22 langues. L'art. 5(d) dispense d'**ajouter** une mention légale mais n'autorise pas à **substituer** son copyright à celui d'autrui sur une base de code qui reste massivement la leur. La ligne indique désormais les deux, ainsi que la licence : `© {{year}} HI-HA — basé sur LearnHouse, © LearnHouse, Inc. — AGPL-3.0`. L'affirmation « aucune mention légale n'a été retirée » portée par une version antérieure de ce fichier était donc inexacte et est retirée. |
| 2026-08-07 | `apps/web/app/orgs/[orgslug]/(withmenu)/layout.tsx` + `LegalFooters.tsx` (`SourceNotice`) — l'offre de code source de l'art. 13 n'était affichée que sur l'écran de connexion et la page d'accueil ; un apprenant connecté, qui passe son temps dans les pages de cours, ne la voyait plus. Elle est désormais présente dans le pied de page de la surface d'apprentissage. |
| 2026-08-07 | `apps/api/src/routers/auth.py` — durcissement de sécurité. `is_request_secure` accordait sa confiance à **toute** adresse IP privée. Sur un réseau Docker partagé (Coolify), chaque conteneur voisin possède une adresse privée et atteint directement le port de l'API, contournant le nginx interne et sa liste blanche `X-Forwarded-Proto`. La confiance est restreinte au loopback, seul chemin de notre proxy légitime. Test de non-régression ajouté (`apps/api/src/tests/routers/test_auth_router.py`). |
| 2026-08-07 | Hygiène de déploiement, sans effet fonctionnel : `docker/nginx.conf` (`server_tokens off`, commentaire rectifié — la correspondance `map` de nginx est insensible à la casse), `apps/web/next.config.js` (`poweredByHeader: false`, identifiant de build), `apps/web/app/api/robots/route.ts` (campus privé sur invitation : plus d'indexation), `.gitignore` et `.dockerignore` (tout fichier `.env` exclu du dépôt **et** du contexte de build, les exemples exceptés — ce dépôt a un miroir public). |
| 2026-08-07 | `apps/api/src/security/features_utils/resolve.py` — correction fonctionnelle. La fonctionnalité `ai` était résolue à partir de la seule configuration de l'**organisation** et ne consultait jamais le réglage d'**instance** `ai_config.is_ai_enabled`. Sur une instance auto-hébergée avec l'IA désactivée, l'interface proposait donc « Créer avec l'IA » (et les icônes IA de l'éditeur) alors que le backend ne pouvait rien servir : la fenêtre s'ouvrait vide. Le réglage d'instance fait désormais autorité — l'IA masquée est réellement masquée. Comportement inchangé lorsque l'IA est activée. |
| 2026-08-07 | `apps/web/proxy.ts` — ergonomie. `/home` est le **sélecteur d'organisation** : il n'a de sens que si l'utilisateur peut appartenir à plusieurs organisations. En tenancy `single` il n'y en a qu'une, et cette page imposait donc un clic inutile à chaque connexion (`login.tsx` comme la redirection des utilisateurs déjà connectés y mènent). En mode `single` uniquement, `/home` doit rediriger vers la liste des cours ; le mode multi-organisations est inchangé. **Effectif après reconstruction sans cache** : `/home` renvoie 307 vers `/courses`. La première tentative semblait inopérante parce que le middleware compilé embarqué dans l'image était périmé — Turbopack réutilisait un cache de compilation, et le chunk chargé ne contenait pas la modification. Diagnostic par comparaison de littéraux dans le chunk (`/subscriptions` présent, `/courses` absent), pas par lecture du code. |
| 2026-08-07 | **Correctif d'isolation entre groupes** — `apps/api/src/services/search/search.py` et `apps/api/src/services/users/usergroups.py`. Le modèle d'autorisation amont isole par **organisation** ; cette instance héberge les formations de clients distincts au sein d'une organisation unique, séparés par `usergroup`. « Membre de la même organisation » n'était donc pas une frontière de confiance, et quatre points fuyaient d'un client à l'autre : la recherche renvoyait le nom **et l'adresse email** de tous les utilisateurs (projection `UserRead`, qui porte `email`), la liste des groupes révélait le nom de tous les clients, le détail et la composition d'un groupe étaient lisibles par un tiers, et la correspondance cours ↔ groupe était exposée. En prime, la création de groupe échappait au contrôle de rôle (le chemin `authorization_verify_if_user_is_author` renvoie `True` pour `create`, sans consulter `usergroups.action_create`). La recherche d'annuaire et les endpoints de groupes sont désormais réservés aux administrateurs d'organisation ; un membre ne voit que ses propres groupes. **Les accès administrateur sont inchangés** (vérifié sur les cinq endpoints) et le cloisonnement des contenus n'est pas modifié — `check_resource_access` n'est pas touché. |
| 2026-08-07 | **Changement de registre d'images : `registry.hi-ha.be` → Docker Hub.** `.gitlab-ci.yml` (fichier ajouté par HI-HA) pousse désormais vers `hihabe/learnhouse`. Une première version de cette entrée annonçait `ghcr.io/sanma88/learnhouse` : cette cible a été abandonnée le jour même, avant tout push, au profit de Docker Hub (licence Pro acquise entre-temps, dépôts privés illimités et pulls authentifiés illimités). L'AGPL art. 13 n'est pas affectée — elle porte sur l'offre de **source**, assurée par le miroir public `github.com/sanma88/learnhouse`, jamais sur le binaire. Motif technique : le registre auto-hébergé est publié derrière un tunnel Cloudflare, dont le plan Free/Pro plafonne à 100 Mo par couche ; quatre couches de cette image dépassent ce plafond (663 Mo pour l'installation apt, 486 Mo pour `uv sync`, 168 Mo pour la copie du build Next.js standalone, 108 Mo pour la base Debian amont — mesuré par `docker history`), et le push échouait en `413 Payload Too Large`. GitLab reste la source du déploiement et le déclencheur. `Dockerfile` — **modification de code amont** : ajout de trois étiquettes OCI (`org.opencontainers.image.source`, `.licenses`, `.description`) sur l'étage `runner`. L'image et sa source vivent désormais à deux endroits distincts (dépôt d'images d'un côté, miroir de la source de l'autre) : `image.source` est le seul élément qui porte le chemin de retour, lisible par `docker inspect` pour quiconque détiendrait l'image sans savoir d'où elle vient. Aucun effet sur l'exécution (les étiquettes sont des métadonnées ; taille de l'image inchangée, 1,94 Go). |
| 2026-08-08 | **Trois défauts découverts au premier déploiement réel**, tous invisibles en test local. (1) `docker/nginx.conf` — les 8 `proxy_pass` visaient `localhost`. Sur un hôte où IPv6 est actif, `localhost` résout `::1` en premier, or uvicorn et Next.js n'écoutent que sur `0.0.0.0` : nginx recevait un refus de connexion et renvoyait **502 sur toutes les routes**, alors que les trois services tournaient normalement. Vérifié dans le conteneur : `http://[::1]:9000` refusé, `http://127.0.0.1:9000` → 200. Le défaut ne se manifeste pas sous Docker Desktop, dont les conteneurs n'ont pas d'IPv6. Les cibles passent en `127.0.0.1` ; `server_name localhost` est inchangé, il ne désigne pas une destination. (2) `apps/api/cli.py` — `_to_async_url` et `_to_sync_url` ne connaissaient que `postgresql://`. Le préfixe court `postgres://`, qui est celui que distribuent Coolify, Heroku et Render, atteignait `create_engine` tel quel et levait `NoSuchModuleError: Can't load plugin: sqlalchemy.dialects:postgres` — SQLAlchemy a retiré ce nom de dialecte en 1.4. Le conteneur redémarrait en boucle (38 fois) sur une erreur qui nomme un greffon plutôt que la chaîne de connexion. `core/events/database.py` faisait déjà cette normalisation pour le chemin des requêtes ; le chemin d'installation ne l'avait pas. (3) `Dockerfile` — l'image ne portait aucun `HEALTHCHECK`, laissant l'orchestrateur en inventer un : celui de Coolify appelle `wget`, absent de cette image, et le conteneur restait indéfiniment en `starting`. Ajouté avec `curl` (installé) et `127.0.0.1`, pour la même raison qu'en (1). |
| 2026-08-08 | `.gitlab-ci.yml` — une variable `COOLIFY_WEBHOOK_URL` absente parvenait à `curl` sous forme d'argument vide, produisant `option : blank argument where content is expected`, message qui ne nomme ni la variable ni le travail à faire. Le travail échoue désormais en disant laquelle manque. |
| 2026-08-08 | **Rectification — le dépôt d'images est PUBLIC, pas privé.** L'entrée du 2026-08-07 et les commentaires de `.gitlab-ci.yml` et du `Dockerfile` annonçaient un dépôt Docker Hub privé. Il a bien été créé ainsi, puis basculé en public le jour du premier déploiement : le `coolify-helper` exécute le pull dans son propre conteneur et ne voit pas le `docker login` de l'hôte, ce qui rendait l'image intirable. Le dépôt privé n'apportait par ailleurs aucune confidentialité réelle — l'image ne contient aucun secret (9 variables d'environnement, toutes structurelles) et sa source est déjà publique sous AGPL. L'art. 13 n'est pas concerné : il porte sur l'offre de **source**, jamais sur le binaire. Les trois emplacements sont corrigés ici conformément à l'art. 5a. |
| 2026-08-10 | **Un seul noeud texte vide effaçait toute la page d'activité, en silence** — ajout de `apps/web/components/Objects/Editor/sanitizeContent.ts`, appelé depuis `apps/web/components/Objects/Activities/DynamicCanva/DynamicCanva.tsx`, `apps/web/components/Objects/Editor/EditorWrapper.tsx` et `apps/web/components/Objects/Editor/EditorPreview.tsx`. Symptôme : deux activités du campus ne s'affichaient plus que comme un encadré blanc, sans message d'erreur. Cause : leur document portait un noeud `{"type":"text","text":""}` ; ProseMirror refuse alors le document **entier**, TipTap intercepte l'exception et lui substitue un document **vide** en ne consignant qu'un `console.warn` — un noeud invalide efface donc toute la page, sans erreur visible pour l'utilisateur. Correctif : ces noeuds sont retirés avant la construction de l'éditeur, obligatoirement par l'option `content` et non par `setContent`, l'extension `NoTextInput` de la vue lecture filtrant toute transaction modifiant le document (`setContent` renvoie `true` sans rien changer). Les trois points de montage sont couverts : vue lecture, vue édition — où un document blanchi puis enregistré écraserait le contenu réel par du vide — et prévisualisation, qui sert l'historique de versions et la modale de conflit, soit précisément les contenus les plus susceptibles de porter ces noeuds, à côté du bouton Restaurer. Le module réunit au passage les deux copies divergentes de `normalizeMarkTypes` (`strong` vers `bold`, `em` vers `italic`) qui coexistaient dans les deux premiers fichiers, et supprime le pré-filtrage par expression régulière d'`EditorWrapper`, plus coûteux que le parcours qu'il évitait puisque l'API renvoie un objet, qu'il faudrait donc sérialiser d'abord. **Portée et limites** : correctif d'affichage côté client uniquement — le contenu stocké n'est pas réécrit et rien n'est rejeté à l'écriture, le seul contrôle de structure côté serveur (`apps/api/src/routers/ai/courseplanning.py`, `validate_prosemirror_content`) vérifiant les types de blocs mais acceptant un noeud texte vide, tandis que le chemin d'enregistrement ordinaire de l'éditeur n'en effectue aucun. Seule cette forme d'invalidité est traitée ; une autre produirait le même écran blanc. `enableContentCheck` de TipTap a été écarté délibérément : il déclencherait `node.check()`, qui échoue sur les noeuds portant à la fois les marques `code` et `bold`, majoritaires dans les activités de ce campus. |
| 2026-08-10 | `apps/web/styles/globals.css` — le code en ligne ne recevait aucun style, en lecture comme en édition. Les trois enveloppes d'éditeur (`.activity-editor-content-wrapper`, `.canva-content-wrapper`, `.editor-preview-wrapper`) ne stylaient que `pre code` : un fragment de code inséré dans une phrase retombait sur le seul reset Tailwind, soit un changement de police et rien d'autre, sans fond ni délimiteur, alors que ces termes désignent ce qui se tape au caractère près — 605 occurrences sur le cours publié. Ajout d'une règle `:not(pre) > code` sur les trois enveloppes : fond noir à 5 %, coins arrondis, rembourrage volontairement minuscule (`padding: 0.05rem 0.1rem`). La règle des cartes de tableau dont elle s'inspire (`.board-editor .board-card-content code`) utilise `0.3rem`, valeur qui convient à une étiquette mais pas à de la prose courante : elle décolle la ponctuation qui suit et recompose les lignes. La règle est posée dans `@layer components` afin qu'un élément portant des classes utilitaires explicites continue de l'emporter — le bloc « code playground » rend son code en ligne avec `bg-neutral-100 text-rose-600 px-1.5`, qu'une règle hors couche écraserait silencieusement à l'échelle de la plateforme. Changement purement visuel : ni contenu ni comportement modifiés, et les surfaces hors de ces trois enveloppes, dont les cartes de tableau, sont inchangées. |
| 2026-08-12 | **Trois retouches de l'interface d'apprentissage.** (1) `apps/web/components/Objects/Editor/Extensions/EmbedObjects/EmbedObjectsComponent.tsx` — un bloc « embed » en mode `code` recevait une hauteur fixe en pixels dans un conteneur `overflow-hidden` : tout contenu dépliable (`<details>`) était coupé net, sans défilement possible, dès qu'il dépassait la hauteur enregistrée. La hauteur de ces blocs suit désormais leur contenu (`height: auto`), replié comme déplié ; le conteneur passe en `flow-root`, contexte de formatage qui retient les marges des enfants sans rien rogner ; la poignée de redimensionnement vertical, devenue sans objet, n'est plus proposée dans ce mode ; et les styles inline posés pendant un redimensionnement sont nettoyés au relâchement, faute de quoi une hauteur fantôme restait figée jusqu'au rechargement. Le mode `url` (iframes) est strictement inchangé : ratio d'affichage ou hauteur fixe, `overflow-hidden` conservé. Vérifié avant déploiement, par balayage de l'API : les sept blocs « embed » de l'instance sont tous en mode `code` et aucun ne contient d'iframe. Les hauteurs enregistrées deviennent sans effet dans ce mode. (2) `apps/web/components/Auth/AuthBrandingPanel.tsx` et `apps/web/components/Auth/AuthMobileHeader.tsx` — la tuile encadrant le logo de l'organisation sur l'écran de connexion imposait un fond blanc, ce qui rend invisible un logo lui-même blanc ; elle passe en fond sombre, le liseré existant étant conservé. **Limite connue** : l'image de repli servie en l'absence de logo d'organisation porte un fond blanc incrusté, si bien que ce changement ne se voit pas sur ce chemin. (3) `apps/web/styles/globals.css` — les listes du contenu d'activité en vue apprenant (`.canva-content-wrapper .ProseMirror ul` et `ol`) n'avaient ni retrait suffisant ni marge verticale, le préréglage de Tailwind remettant les deux à zéro ; retrait porté à `1.5rem` et marge verticale à `1rem`. La valeur intermédiaire d'abord retenue (`0.5rem`) resserrait en réalité l'espacement au lieu de l'aérer, la règle générale `.ProseMirror > * + *` fournissant déjà `0.75em` depuis une couche de moindre priorité. Portée limitée à la surface de lecture : l'éditeur et la prévisualisation ne sont pas touchés. |
| 2026-08-12 | Ajout de `TODO.md` (travaux prévus sur ce fork et son exploitation). Aucune modification du code ni des mentions légales. |
| 2026-09-01 | **Bump de sécurité : `next` 16.2.9 → 16.3.4 et `sharp` 0.35.2 → 0.35.4** (`apps/web/package.json` : specs `^16.3.4` / `^0.35.4`, `eslint-config-next` 16.2.11 → 16.3.4). Corrige la **RCE non authentifiée via fichiers AVIF** de l'API d'optimisation d'images (GHSA-2xp9-vwfh-vxw4 ; cause racine : heap overflow dans libheif ≤ 1.23.1 embarqué par sharp, GHSA-g89c-p67h-r497, CVSS 9.8 — sharp 0.35.4 = libvips 1.3.3 = libheif 1.23.2 corrigé, et les entrées imbriquées `next/sharp@0.34.5` vulnérables disparaissent du lock), et couvre au passage les **9 advisories corrigées en 16.2.11** (dont 2 SSRF high, bypass proxy/middleware Turbopack high, DoS Server Actions high) auxquelles la prod, restée en 16.2.9, était exposée. La seconde critique du même bulletin (CVE-2026-75604, RCE sur serveurs Windows) est **non applicable** (conteneur Linux). L'image Docker est construite depuis `apps/web/bun.lock` (**régénéré avec bun 1.3.14**, la version épinglée du Dockerfile) ; `apps/web/pnpm-lock.yaml` (héritage upstream, jamais lu par le build) mis à jour par cohérence. `apps/web/tsconfig.json` : ajout de `"ignoreDeprecations": "6.0"` — la phase de type-check de next 16.3.x rend **fatale** la dépréciation TS5101 (`baseUrl` déprécié par TypeScript 6.0.3, retrait prévu en 7.0) que 16.2.9 tolérait ; contournement minimal, la migration réelle de `baseUrl`/`paths` est reportée au prochain merge upstream. |
| 2026-09-01 | **`Dockerfile` (stage `frontend-builder`) : `next build` exécuté sous Node réel.** Le pipeline CI #604 échouait en exit 139 : Bun 1.3.14 (musl) segfaultait au teardown de `next build` 16.3.4, après une compilation pourtant réussie (« panic: Segmentation fault — a bug in Bun »). L'image `oven/bun` ne fournit qu'un shim `node`→bun (`/usr/local/bun-node-fallback-bin`, en fin de `PATH`) : le shebang `#!/usr/bin/env node` de `next` retombait donc sur le runtime Bun. Correctif : `apk add nodejs` (Node 22, même major que le runtime du stage final) dans le seul stage de build frontend ; `/usr/bin/node` précède le shim, `bun run build` exécute désormais `next` sous Node réel. Bun reste l'outil d'installation des dépendances (pin 1.3.14 inchangé). |
| 2026-09-01 | **Correctif CSP : aperçus MagicBlock (iframes `blob:`) blancs en navigation SPA** — `apps/web/next.config.js`, bloc global d'en-têtes `/:path*` : `X-Frame-Options` passe de `DENY` à `SAMEORIGIN` et `Content-Security-Policy` de `frame-ancestors 'none'` à `frame-ancestors 'self'`. Symptôme : les iframes d'aperçu MagicBlock (`src` en `blob:`) restaient blanches tant que la page n'était pas rechargée, avec en console « Refused to load blob:… frame-ancestors ». Cause : en navigation SPA, la CSP du document d'**entrée** gouverne toute la session côté client, et les documents `blob:` en héritent — le `frame-ancestors 'none'` global refusait donc l'encadrement même same-origin de ces aperçus ; un F5 depuis `/course/*` fonctionnait parce que l'exception `'self'` de cette route devenait alors la CSP d'entrée. Correctif minimal au niveau global ; les exceptions par route existantes (`/embed/*` → `*`, `/api/scorm/*`, routes ressources) sont inchangées. La **protection anti-clickjacking externe est conservée** : `'self'` + `SAMEORIGIN` continuent d'interdire tout encadrement par une origine tierce, seul l'encadrement par la propre origine du site devient permis. Vérifié qu'aucune autre couche ne pose ces en-têtes (`docker/nginx.conf`, `apps/web/proxy.ts`, `apps/web/server-wrapper.js`, aucun middleware — seul `docs/next.config.mjs`, application de documentation séparée, pose son propre `DENY`, hors périmètre). |
| 2026-09-01 | **Merge upstream : release `1.3.5`** (`upstream/main` `7369713e`, 129 commits depuis le point de fork `1.3.4`/`fb519651`). Apporte notamment : invalidation des sessions au changement de mot de passe et au logout (#1036), durcissement API (#1003 — authz/validation en couche service, anti-SSRF des link-previews, vérification des webhooks Resend, endpoints internes qui échouent fermés), surface superadmin réservée à l'Enterprise (#1025), tiptap 3.28 → 3.30.2, quiz à réponses multiples, bloc H5P, RTL complet, nom d'expéditeur par organisation, emails « nudge » (opt-in, inertes sans `LEARNHOUSE_NUDGES_ENABLED`). Résolutions de merge notables côté fork : (1) `apps/web/package.json`/`bun.lock` — conservation de `next ^16.3.4`, `sharp ^0.35.4`, `eslint-config-next 16.3.4` (l'upstream restait sur des versions vulnérables 16.2.9/16.3.1) avec adoption de `motion ^13` upstream, lockfile **régénéré avec bun 1.4.0** ; (2) `Dockerfile` — tous les stages bun épinglés `oven/bun:1.4.0-alpine` (le `bun.lock` régénéré est en lockfileVersion 3, illisible par bun ≤ 1.3.14) et **conservation du `apk add nodejs`** du stage `frontend-builder` (voir entrée CI #604) ; (3) correctif sémantique : le code nudge upstream référençait la constante `LOGO_SVG` supprimée par le rebrand — remplacée par `_brand_logo_html()` (`apps/api/src/services/users/emails.py`), sans quoi l'API ne démarrait pas ; (4) expéditeur email : adoption du `format_sender()` upstream avec repli plateforme sur `site_name` configuré au lieu du nom produit upstream (`apps/api/src/services/email/utils.py`) ; (5) `apps/web/tsconfig.json` — adoption de la migration upstream (suppression de `baseUrl`, `paths` relatifs), le contournement `ignoreDeprecations` n'est plus nécessaire et est retiré ; (6) `apps/web/pnpm-lock.yaml` supprimé (suppression upstream acceptée — jamais lu par le build, remplacé par la CI lockfiles upstream) ; (7) conflits d'identité (config.yaml, page.tsx, CertificatePreview, Watermark, EmbedObjects, globals.css, .gitignore) résolus en gardant l'identité HI-HA + les classes RTL upstream. Les customisations listées dans ce NOTICE ont été re-vérifiées une à une après merge. |
| 2026-09-01 | **Rebrand des emails « nudge » upstream** — les 20 fichiers `apps/api/src/services/email/nudge_translations/*.py` introduits par la release 1.3.5 portent chacun une chaîne produit (« …on LearnHouse » et équivalents localisés) : remplacées par « HI-HA » selon la convention déjà appliquée aux 184 chaînes de `email/translations.py` (suffixes agglutinés conservés, ex. turc « HI-HA'ta »). `DEFAULT_SENDER_NAME` du nouveau `email/sender.py` upstream reste inchangé : il est neutralisé par le repli plateforme sur `site_name` posé dans `email/utils.py` (voir entrée du merge). Également : les 2 chaînes UI `session_sharing_label`/`session_sharing_hint` (nouvelles en 1.3.5, présentes uniquement dans `locales/en.json` et `locales/ar.json`) codaient « learnhouse.io » en dur — alignées sur la formulation neutre « the platform » du `defaultValue` du composant `OrgSignInMethods.tsx`. |
| 2026-09-01 | **`docs/package.json` : `next` 16.3.1 → 16.3.4** (+ `docs/bun.lock` régénéré avec bun 1.4.0, frozen install vérifié). L'upstream avait monté la doc à 16.3.1 seulement, version encore exposée au bulletin AVIF corrigé en 16.3.3/16.3.4 (voir entrée du bump du 2026-09-01). Application de documentation séparée, non buildée dans l'image Docker — mise en conformité par cohérence (P1 de la revue U3). |
| 2026-09-01 | **Migrations de schéma appliquées au boot** — ajout de `apps/api/boot_migrations.py`, appelé par `docker/start.sh` (image campus, avant le lancement des services) et `apps/api/docker-entrypoint.sh` (image API seule, avant uvicorn). Le démarrage applicatif ne fait que `SQLModel.metadata.create_all`, qui crée les tables manquantes mais n'émet jamais d'`ALTER TABLE` : la colonne `organization.is_demo` (migration `c7d8e9f0a1b2`, release 1.3.5, lue en 36 points du code) n'aurait jamais existé sur une base déjà installée → `UndefinedColumn` sur pratiquement chaque requête authentifiée (bloquant B1 de la revue du merge). Un `alembic upgrade heads` inconditionnel ne suffisait pas : la chaîne amont ne sait pas construire une base vierge (la migration initiale `df2981bf24dd` altère des tables supposées exister) et une base installée par `create_all` seul n'a pas de table `alembic_version`. Le script distingue donc trois états : base suivie → `alembic upgrade heads` (pluriel, robuste aux têtes multiples — le graphe réel n'a qu'une tête, `c7d8e9f0a1b2`, les anciennes branches fusionnant en `e6f7a8b9c0d1_merge_heads` : l'affirmation « 4 têtes » de la revue venait d'une lecture par expression régulière qui manquait le `down_revision` en tuple multi-lignes, réfutée par `alembic heads` dans l'image construite) ; base installée non suivie (le cas de la production, installée le 2026-08-08) → adoption unique par `stamp` de la tête du code ayant construit le schéma (`t6u7v8w9x0y1`, état vérifié au commit `68e35317`) puis `upgrade heads`, qui n'applique que `b1n2u3d4g5e6` et `c7d8e9f0a1b2` ; base vierge → `stamp heads`, le `create_all` du démarrage construisant le schéma courant. Un échec de migration est un échec de boot : le healthcheck reste rouge et l'orchestrateur conserve l'ancien conteneur en rolling update. Prouvé en local sur Postgres jetable : chemin base vierge et chemin d'adoption (schéma de production reconstitué sans `is_demo` ni `alembic_version` → colonne créée par alembic, 4 têtes enregistrées, API en 200). |
| 2026-09-01 | `Dockerfile` (stage final) : **installation de bun épinglée** — `curl -fsSL https://bun.sh/install \| bash` installait « la dernière version publiée » (le mode de panne du 2026-08-20, et un décalage réel a été observé : 1.3.14 servi par couche cachée alors que les stages de build sont en 1.4.0). Le script d'installation accepte un tag précis en premier argument → `bash -s "bun-v1.4.0"`, aligné sur les stages de build. L'installation reste requise : `bun install --production` (dépendances du serveur collab) en dépend. Condition pré-existante amont, pas une régression du merge 1.3.5. |
| 2026-09-01 | **Rebrand : fichier de codes de secours 2FA (en/ar)** — la release 1.3.5 introduit la clé de traduction `codes_file_header` dans `apps/web/locales/en.json` et `ar.json` uniquement ; une clé présente masque le `defaultValue` HI-HA du composant `AccountSecurity.tsx`, et le fichier `.txt` de codes de secours téléchargé par un utilisateur en/ar redevenait « LearnHouse » (régression M1 de la revue du merge — même schéma que les 2 chaînes `session_sharing_*` déjà traitées, celle-ci avait été manquée). Valeur alignée sur le branding HI-HA dans les deux locales, cohérente avec les 184 chaînes déjà rebrandées. |
| 2026-09-01 | `apps/web/docker-entrypoint.sh` : retour de `exec env bun server-wrapper.js` (introduit par le merge 1.3.5) à `exec env node server-wrapper.js`. **Sans effet sur l'image campus** : ce fichier n'est pas copié par le `Dockerfile` racine (le web y est servi sous Node via pm2, `docker/start.sh`) ; il n'est l'entrypoint que de l'image par-application `apps/web/Dockerfile`, que la CI ne construit pas. Aligné par cohérence avec la politique runner Node du fork. |

## Code source (AGPL art. 13)

Toute personne utilisant ce logiciel via le réseau peut obtenir le code source
de la version exécutée, y compris ces modifications, depuis le miroir public :
**https://github.com/sanma88/learnhouse** (branche `hiha`).
